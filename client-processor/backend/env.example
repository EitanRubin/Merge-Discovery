# Server Configuration
PORT=3001

# Target API URL - where processed results will be sent
# This is REQUIRED - the server will not start without it
TARGET_API_URL=https://your-target-api.com/endpoint

# Optional: Request timeout in milliseconds (default: 30000)
REQUEST_TIMEOUT=30000

# Optional: Enable verbose logging (default: false)
VERBOSE_LOGGING=false

# =====================================================
# INSTRUCTIONS:
# 1. Copy this file and rename it to ".env"
# 2. Update TARGET_API_URL with your actual endpoint
# =====================================================

